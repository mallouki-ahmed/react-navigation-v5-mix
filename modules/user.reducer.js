const INITIAL_STATE = {
  user: {},
  possible: ['Allie', 'Gator', 'Lizzie', 'Reptar'],
};

const users = (state = INITIAL_STATE, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default users;
